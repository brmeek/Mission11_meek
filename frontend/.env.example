# Used by production builds for absolute API calls (optional).
# Leave empty to use same-origin /api.
VITE_API_BASE_URL=

# Used only by Vite dev server proxy in `npm run dev`.
VITE_API_PROXY_TARGET=http://localhost:52877
